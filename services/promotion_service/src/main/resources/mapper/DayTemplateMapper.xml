<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.foodapp.promotion_service.persistence.repository.DayTemplateRepository">

    <resultMap id="DayTemplateResultMap"
               type="com.foodapp.promotion_service.persistence.entity.DayTemplateEntity">
        <id property="id" column="id" typeHandler="com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler"/>

        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="ruleJson" column="rule_json"
                typeHandler="com.foodapp.promotion_service.persistence.typehandler.JsonPromotionRulesTypeHandler"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="create" parameterType="com.foodapp.promotion_service.persistence.entity.DayTemplateEntity">
        INSERT INTO day_template (id, name, description, rule_json, created_by, created_at)
        VALUES (
                   #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid,
                   #{name},
                   #{description},
                   #{ruleJson, typeHandler=com.foodapp.promotion_service.persistence.typehandler.JsonPromotionRulesTypeHandler}::jsonb,
                   #{createdBy},
                   #{createdAt}
               )
    </insert>

    <update id="update">
        UPDATE day_template
        SET
            name = #{name},
            description = #{description},
            rule_json = #{ruleJson, typeHandler=com.foodapp.promotion_service.persistence.typehandler.JsonPromotionRulesTypeHandler}::jsonb
        WHERE id = #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid
    </update>

    <update id="partialUpdate">
        UPDATE day_template
        <set>
            <if test="name != null"> name = #{name}, </if>
            <if test="description != null"> description = #{description}, </if>
            <if test="ruleJson != null">
                rule_json = #{ruleJson, typeHandler=com.foodapp.promotion_service.persistence.typehandler.JsonPromotionRulesTypeHandler}::jsonb,
            </if>
        </set>
        WHERE id = #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid
    </update>

    <delete id="delete">
        DELETE FROM day_template
        WHERE id = #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid
    </delete>

    <delete id="bulkDelete">
        DELETE FROM day_template
        WHERE id IN
        <foreach collection="ids" item="id" open='(' separator="," close=")">
            #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid
        </foreach>
    </delete>

    <select id="findById" resultMap="DayTemplateResultMap">
        SELECT * FROM day_template
        WHERE id = #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid
    </select>

    <select id="findAll" resultMap="DayTemplateResultMap">
        SELECT * FROM day_template ORDER BY created_at DESC
    </select>

    <select id="existsById" resultType="boolean">
        SELECT COUNT(*) > 0 FROM day_template
        WHERE id = #{id, typeHandler=com.foodapp.promotion_service.persistence.typehandler.UuidTypeHandler}::uuid
    </select>
</mapper>